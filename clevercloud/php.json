{
  "deploy": {
    "webroot": "/public"
  },
  "hooks": {
    "postBuild": "php artisan key:generate --force && php artisan migrate --force && php artisan config:cache && php artisan storage:link"
  }
}